
1. Create a Supabase project at supabase.com → New project (free)
2. Run the schema — paste supabase/schema.sql into the Supabase SQL editor and click Run
3. Set env vars — copy .env.local.example to .env.local and fill in your project URL + anon key from Supabase Settings → API
